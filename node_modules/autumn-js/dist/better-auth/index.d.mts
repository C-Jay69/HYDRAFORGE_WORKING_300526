import { R as RouteName, c as createAutumnEndpoint } from './createAutumnEndpoint-p1QlSPE4.mjs';
import { AutumnOptions } from './types.mjs';
import 'zod';
import 'better-auth';

/** Type for a single autumn endpoint */
type AutumnEndpoint<T extends RouteName> = ReturnType<typeof createAutumnEndpoint<T>>;
/** Autumn endpoints type - maps each route to its endpoint type */
type AutumnEndpoints = {
    [K in RouteName]: AutumnEndpoint<K>;
};
/** Autumn plugin return type */
type AutumnPlugin = {
    id: "autumn";
    endpoints: AutumnEndpoints;
};
declare function autumn(options?: AutumnOptions): AutumnPlugin;

export { type AutumnEndpoints, AutumnOptions, type AutumnPlugin, autumn };

import { RouterContext } from 'rou3';
import { R as RouteDefinition } from '../../routeTypes-B7YXq83l.js';
import '../../authTypes-BCoyMOAj.js';
import 'zod/v4';
import '../types/responseTypes.js';

type RouteMatch = {
    route: RouteDefinition;
};
/** Build a rou3 router from route definitions */
declare const buildRouter: ({ pathPrefix, routes, }: {
    pathPrefix: string;
    routes: RouteDefinition[];
}) => RouterContext<RouteMatch>;

export { type RouteMatch, buildRouter };
